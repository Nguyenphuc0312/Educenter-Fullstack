# Payment QR assets

Place the center bank QR image here:

```text
frontend/public/payment-qr/techcombank-qr.jpg
```

The student tuition payment screen uses `/payment-qr/techcombank-qr.jpg` as the static fallback QR.
If VietQR environment variables are configured, the app generates a dynamic QR with amount and invoice content instead.
